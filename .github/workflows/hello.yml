name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "HOLA PROGRAMADORES!"
      - name: Dime la fecha
        run: date
      - name: Dime la fecha
        run: date
      - run: ls -l
      - name: Hello Worldoworld!
        uses: srt32/yaaas@master
          
